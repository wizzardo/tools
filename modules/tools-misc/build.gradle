dependencies {
    compile project(':modules:tools-reflection')
    compile project(':modules:tools-interfaces')
}

archivesBaseName = "tools-misc"
ext {
    descriptionString = 'misc tools'
}
apply from: "${rootProject.projectDir}/gradle/sonatype.gradle"