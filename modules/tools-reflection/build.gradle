dependencies {
    compile project(':modules:tools-exception')
    compile project(':modules:tools-interfaces')
}

archivesBaseName = "tools-reflection"
ext {
    descriptionString = 'reflection tools'
}
apply from: "${rootProject.projectDir}/gradle/sonatype.gradle"