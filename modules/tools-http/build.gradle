dependencies {
    compile project(':modules:tools-exception')
    compile project(':modules:tools-security')
    compile project(':modules:tools-reflection')
}

archivesBaseName = "tools-http"
ext {
    descriptionString = 'http tools'
}
apply from: "${rootProject.projectDir}/gradle/sonatype.gradle"

